{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Run server",
			"command": "start.bat"
		},
		{
			"label": "Run Bun tests in Docker",
			"type": "shell",
			"command": "docker compose run --rm main bun test --json",
			"group": {
				"kind": "test",
				"isDefault": true
			},
			"problemMatcher": []
		}
	]
}
